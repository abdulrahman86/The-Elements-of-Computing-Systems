// 

CHIP TESTDFF {
    IN in;
    OUT out;


    PARTS:
    // Put your code here.
	Mux(a=in, b=false, sel=true, out=R);
	Nand(a=clk, b=in, out=SStar);
	Nand(a=clk, b=R, out=RStart);
	Nand(a=SStar, b=NotQ, out=Q, out=out);
	Nand(a=RStar, b=Q, out=NotQ);

	CLOCKED clk;
}
